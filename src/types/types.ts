export interface EmployeeDTO {
    name: string,
    position: string,
    salary: number,
    hired: boolean
}

export interface ProductDTO {
    id: string,
    name: string,
    description: string,
    color: string,
    weight: number,
    type: string,
    price: number,
    dateRegister: Date
}